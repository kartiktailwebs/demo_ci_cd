on:
  push:
    branches:
      - main

name: 🚀 Deploy website on push

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v1
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.0.0
      with:
        ssh-private-key: /Users/kartik/.ssh/tw_staging
        ssh-username: gaganbabber
        ssh-host: 104.155.230.1
        ssh-port: 22
        server-dir: /home/gaganbabber/rails/staging/
