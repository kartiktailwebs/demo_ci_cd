name: Deploy Code to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup SSH
        uses: appleboy/ssh-action@master
        with:
          host: 104.155.230.1
          username: gaganbabber
          key: ${{ secrets.tw_staging }}
          port: 22

      - name: Deploy to Server
        run: |
          # Add your deployment commands here, e.g., rsync, scp, etc.
          # Example using rsync:
          rsync -r --delete /Users/kartik/Projects/4_Ruby/0_study/2_Rails_Projects_Demo/demo-ci-cd gaganbabber@104.155.230.1:/home/gaganbabber/rails/staging/
